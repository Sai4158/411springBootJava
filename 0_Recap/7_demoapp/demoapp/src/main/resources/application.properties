spring.application.name=demoapp

# ---------- MySQL ----------
spring.datasource.url=jdbc:mysql://localhost:3306/dog_park_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=sqlroot4158

# ---------- JPA ----------
# Leave the tables you created exactly as-is; don’t let Hibernate touch them.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true              

# ---------- SQL scripts ----------
# Run schema.sql (your DDL / inserts) automatically every boot
spring.sql.init.mode=always          
